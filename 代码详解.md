# 代码详解 — TC6414 Assignment 1 Notebook

> 本文件逐行解释 notebook.ipynb 里的每一段代码，包括：每行在做什么、变量的含义、为什么这样写。
> 适合零基础阅读。

---

## 第一部分：Setup（环境准备）

```python
import os
from pathlib import Path
import warnings
```

| 语句 | 含义 |
|---|---|
| `import os` | 导入操作系统工具，这里只用来设置一个环境变量 |
| `from pathlib import Path` | 导入路径工具，`Path("data")` 比写字符串 `"data/"` 更安全跨平台 |
| `import warnings` | 导入警告控制模块 |

```python
os.environ.setdefault("MPLCONFIGDIR", str(Path.cwd() / "output" / ".matplotlib"))
```
- `os.environ` 是系统环境变量的字典
- `MPLCONFIGDIR` 告诉 Matplotlib（画图库）把缓存文件放在哪里
- `Path.cwd()` 是当前工作目录，`/ "output" / ".matplotlib"` 是拼接子路径
- 效果：把 Matplotlib 的字体缓存放到 `output/.matplotlib/`，避免报权限错误

```python
warnings.filterwarnings("ignore")
```
- 屏蔽所有警告信息，让输出更干净
- 不影响代码运行结果，只是不显示黄色提示

---

```python
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
```

| 库 | 别名 | 用途 |
|---|---|---|
| `pandas` | `pd` | 处理表格数据（DataFrame），读 CSV、筛选、合并 |
| `numpy` | `np` | 数学计算，比如 `np.log1p()`、`np.cumsum()` |
| `matplotlib.pyplot` | `plt` | 底层画图工具，控制图的大小、保存 |
| `seaborn` | `sns` | 基于 matplotlib 的高级画图库，一行代码出漂亮图 |

---

```python
from sklearn.preprocessing import StandardScaler
from sklearn.decomposition import PCA
from sklearn.feature_selection import mutual_info_classif, RFE
from sklearn.linear_model import LogisticRegression
from sklearn.model_selection import cross_val_score, StratifiedKFold
from sklearn.metrics import make_scorer, accuracy_score, precision_score, recall_score, f1_score
```

`sklearn`（scikit-learn）是 Python 最主流的机器学习库，这里从中导入：

| 导入内容 | 用途 |
|---|---|
| `StandardScaler` | 标准化（把数据变成均值=0、标准差=1） |
| `PCA` | 主成分分析，用于降维 |
| `mutual_info_classif` | 互信息特征评分（特征选择方法1） |
| `RFE` | 递归特征消除（特征选择方法2） |
| `LogisticRegression` | 逻辑回归模型（本次作业的分类器） |
| `cross_val_score` | 交叉验证评分 |
| `StratifiedKFold` | 分层K折交叉验证（保证每折中正负样本比例相同） |
| `make_scorer` | 把评价函数包装成 sklearn 能用的格式 |
| `accuracy_score` | 准确率 |
| `precision_score` | 精确率 |
| `recall_score` | 召回率 |
| `f1_score` | F1分数 |

---

```python
sns.set_style("whitegrid")
plt.rcParams["figure.dpi"] = 120
```

- `sns.set_style("whitegrid")` — 设置图表背景为白色+网格线风格
- `plt.rcParams["figure.dpi"] = 120` — 设置图片分辨率为120 DPI（图更清晰）

---

```python
DATA_DIR   = Path("data")
OUTPUT_DIR = Path("output")
FIG_DIR    = OUTPUT_DIR / "figures"
OUTPUT_DIR.mkdir(exist_ok=True)
FIG_DIR.mkdir(parents=True, exist_ok=True)
```

| 变量 | 含义 |
|---|---|
| `DATA_DIR` | 数据目录，指向 `data/` 文件夹 |
| `OUTPUT_DIR` | 输出目录，指向 `output/` 文件夹 |
| `FIG_DIR` | 图片目录，指向 `output/figures/` 文件夹 |

- `mkdir(exist_ok=True)` — 创建文件夹，如果已经存在就不报错
- `parents=True` — 如果父目录（`output/`）不存在也一起创建

---

```python
missing_zero_cols = ["Glucose", "BloodPressure", "SkinThickness", "Insulin", "BMI"]
```
- **变量名**：`missing_zero_cols`（含义：用零代表缺失值的列）
- 这5列在医学上不可能是0（血糖不能是0，血压不能是0，否则人就死了）
- 所以数据里的0实际上是"没有记录"，是缺失值的代号

```python
feature_cols = ["Pregnancies", "Glucose", "BloodPressure", "SkinThickness",
                "Insulin", "BMI", "DiabetesPedigreeFunction", "Age"]
```
- **变量名**：`feature_cols`（含义：特征列）
- 这8列是我们用来预测糖尿病的输入变量（自变量 X）
- 不包含 `Outcome`（因为那是我们要预测的目标）

```python
target_col = "Outcome"
```
- **变量名**：`target_col`（含义：目标列）
- `Outcome` 列：0 = 没有糖尿病，1 = 有糖尿病
- 这是我们要预测的输出变量（因变量 y）

---

## 第二部分：加载数据

```python
df_pima      = pd.read_csv(DATA_DIR / "pima.csv")
df_frankfurt = pd.read_csv(DATA_DIR / "frankfurt.csv")
```

| 变量 | 含义 |
|---|---|
| `df_pima` | PIMA印第安人糖尿病数据集，768行×9列，来自美国 |
| `df_frankfurt` | 法兰克福医院糖尿病数据集，2000行×9列，来自德国 |

- `pd.read_csv()` — 读取 CSV 文件，返回一个 DataFrame（类似 Excel 表格）
- `DATA_DIR / "pima.csv"` — 拼接路径，等价于 `"data/pima.csv"`

探索数据的方法：

| 方法 | 作用 |
|---|---|
| `df.info()` | 显示列名、数据类型、非空值数量、内存占用 |
| `df.head()` | 显示前5行，快速看数据长什么样 |
| `df.describe()` | 显示统计摘要：count、mean、std、min、25%、50%、75%、max |

---

## 第三部分：处理缺失值（Section 3.1）

```python
zero_counts_before = pd.DataFrame({
    "PIMA_zero_count": (df_pima[missing_zero_cols] == 0).sum(),
    "Frankfurt_zero_count": (df_frankfurt[missing_zero_cols] == 0).sum()
})
```
- `df_pima[missing_zero_cols]` — 只取那5列
- `== 0` — 每个格子是否等于0，返回 True/False 表格
- `.sum()` — True=1、False=0，加起来就是0的个数
- `pd.DataFrame({...})` — 把两列数据合并成一个表
- **变量** `zero_counts_before`：记录替换前每列有多少个0

```python
df_pima[missing_zero_cols] = df_pima[missing_zero_cols].replace(0, np.nan)
df_frankfurt[missing_zero_cols] = df_frankfurt[missing_zero_cols].replace(0, np.nan)
```
- `.replace(0, np.nan)` — 把所有0替换成 `NaN`（Not a Number，表示缺失）
- 为什么要替换？因为后面的中位数填充和模型需要识别"缺失"才能正确处理

```python
missing_counts_after = pd.DataFrame({
    "PIMA_missing": df_pima[missing_zero_cols].isna().sum(),
    "Frankfurt_missing": df_frankfurt[missing_zero_cols].isna().sum()
})
```
- `.isna()` — 判断每个格子是否是 NaN，返回 True/False
- `.sum()` — 数出每列有多少个 NaN
- **变量** `missing_counts_after`：记录替换后每列有多少个缺失值（应该和之前的0数量一样）

---

**画热力图：**

```python
fig, axes = plt.subplots(1, 2, figsize=(14, 5), sharey=True)
```
- `plt.subplots(1, 2)` — 创建1行2列的画布（两张并排的图）
- `figsize=(14, 5)` — 画布宽14英寸、高5英寸
- `sharey=True` — 两张图共享Y轴（行索引对齐）
- **变量** `fig`：整个画布对象
- **变量** `axes`：长度为2的数组，`axes[0]`是左图，`axes[1]`是右图

```python
sns.heatmap(df_pima[missing_zero_cols].isna(), cbar=True, ax=axes[0],
            cmap="YlOrRd", yticklabels=False)
```
- `df_pima[missing_zero_cols].isna()` — 生成一个True/False矩阵（True=缺失）
- `sns.heatmap()` — 把True/False矩阵画成颜色热力图
- `cmap="YlOrRd"` — 颜色方案：黄→橙→红（缺失处显示亮色）
- `yticklabels=False` — 不显示Y轴的行号（行太多显示没意义）
- `ax=axes[0]` — 画在左边那张子图上

```python
plt.savefig(FIG_DIR / "fig_3_1_1_missing_heatmap.png", bbox_inches="tight")
```
- 把图保存到 `output/figures/fig_3_1_1_missing_heatmap.png`
- `bbox_inches="tight"` — 裁掉多余白边

---

**中位数填充：**

```python
for col in missing_zero_cols:
    df_pima[col] = df_pima[col].fillna(df_pima[col].median())
    df_frankfurt[col] = df_frankfurt[col].fillna(df_frankfurt[col].median())
```
- `for col in missing_zero_cols` — 依次处理那5列
- `df_pima[col].median()` — 计算该列的中位数（中间值）
- `.fillna(中位数)` — 把所有 NaN 替换成该列的中位数
- 为什么用中位数不用均值？因为中位数不受极端值影响（比如Insulin有很多极大值，均值会被拉高）
- 两个数据集**分别**填充，用各自的中位数，保留各自数据集的特征

---

## 第四部分：数据整合（Section 3.2）

```python
df_pima["source"] = "PIMA"
df_frankfurt["source"] = "Frankfurt"
```
- 给每个数据集添加一列 `source`，标记数据来源
- 合并后可以知道每行数据来自哪个数据集

```python
df_combined = pd.concat([df_pima, df_frankfurt], axis=0, ignore_index=True)
```
- `pd.concat([...], axis=0)` — 上下拼接两个DataFrame（纵向拼接）
- `ignore_index=True` — 重置行号从0开始（否则PIMA行号0-767，Frankfurt行号0-1999，会重复）
- **变量** `df_combined`：合并后的完整数据集，2768行×10列

```python
df_model = df_combined.drop(columns="source").copy()
```
- `.drop(columns="source")` — 删除 `source` 列（建模不需要它）
- `.copy()` — 创建副本，避免修改 `df_combined`（防止连锁修改的bug）
- **变量** `df_model`：用于建模的最终数据集，2768行×9列

---

## 第五部分：数据变换与标准化（Section 3.3 & 3.4）

```python
X_original = df_model[feature_cols].copy()
y = df_model[target_col].copy()
```
- **变量** `X_original`：所有特征（8列），机器学习里习惯用 X 表示输入
- **变量** `y`：目标变量（Outcome列），习惯用 y 表示输出/标签

```python
log_cols = ["Insulin", "DiabetesPedigreeFunction"]
X_transformed = X_original.copy()
before_log = X_transformed[log_cols].copy()
```
- **变量** `log_cols`：需要做对数变换的列（这两列数据分布严重偏斜）
- **变量** `X_transformed`：变换后的特征矩阵（先复制，再修改）
- **变量** `before_log`：变换前的数据，保存下来用于画图对比

```python
for col in log_cols:
    X_transformed[col] = np.log1p(X_transformed[col])
```
- `np.log1p(x)` — 计算 `ln(x + 1)`（自然对数加1）
- 加1是为了处理0值（`ln(0)` 是无穷大，`ln(0+1)=0` 没问题）
- 效果：把右偏（长尾在右边）的分布压缩成更对称的分布

```python
after_log = X_transformed[log_cols].copy()
```
- **变量** `after_log`：变换后的数据，用于画图对比

---

**画变换前后对比图：**

```python
palette = sns.color_palette("tab10")
color_before = palette[0]   # tab blue（蓝色）
color_after  = palette[1]   # tab orange（橙色）
```
- `sns.color_palette("tab10")` — 获取10种标准颜色的列表
- **变量** `color_before`：变换前用蓝色
- **变量** `color_after`：变换后用橙色

```python
fig, axes = plt.subplots(2, 2, figsize=(14, 8))
for row, col in enumerate(log_cols):
    sns.histplot(before_log[col], kde=True, ax=axes[row, 0], ...)
    sns.histplot(after_log[col],  kde=True, ax=axes[row, 1], ...)
```
- `plt.subplots(2, 2)` — 2行2列，共4张子图
- `enumerate(log_cols)` — 同时获得索引和值，第0次`row=0,col="Insulin"`，第1次`row=1,col="DiabetesPedigreeFunction"`
- `axes[row, 0]`：第row行第0列的子图（变换前）
- `axes[row, 1]`：第row行第1列的子图（变换后）
- `kde=True` — 同时画核密度估计曲线（平滑的概率分布曲线）

---

**标准化：**

```python
scaler = StandardScaler()
X_scaled_array = scaler.fit_transform(X_transformed[feature_cols])
X_scaled = pd.DataFrame(X_scaled_array, columns=feature_cols)
```
- **变量** `scaler`：StandardScaler对象，负责标准化
- `fit_transform()` — 先"学习"每列的均值和标准差（fit），再变换数据（transform）
- 变换公式：`z = (x - 均值) / 标准差`，结果是均值=0，标准差=1
- `X_scaled_array` — 变换后是numpy数组（没有列名）
- `pd.DataFrame(X_scaled_array, columns=feature_cols)` — 转回DataFrame并加上列名
- **变量** `X_scaled`：标准化后的特征矩阵，这是后续所有步骤的输入

```python
before_melted = X_transformed[feature_cols].melt(var_name="Feature", value_name="Value")
after_melted  = X_scaled.melt(var_name="Feature", value_name="Value")
```
- `.melt()` — 把"宽表"转成"长表"
  - 宽表：每列是一个特征，8列
  - 长表：两列（Feature名字, Value数值），行数 = 原来的行数×8
- 为什么要转？`sns.boxplot` 需要长表格式才能一次画所有特征的箱线图
- **变量** `before_melted`：标准化前的长表数据
- **变量** `after_melted`：标准化后的长表数据

---

## 第六部分：PCA（Section 3.5）

```python
pca = PCA()
pca.fit(X_scaled)
```
- **变量** `pca`：PCA对象
- `PCA()` — 不指定组件数量，计算所有8个主成分
- `pca.fit(X_scaled)` — 让PCA"学习"数据的方差结构（不改变数据本身）

```python
explained_variance = pca.explained_variance_ratio_
cumulative_variance = np.cumsum(explained_variance)
n_components_95 = int(np.argmax(cumulative_variance >= 0.95) + 1)
```
- **变量** `explained_variance`：数组，每个主成分解释的方差比例
  - 例如：`[0.2881, 0.1829, 0.1360, ...]`
- **变量** `cumulative_variance`：累计方差，`np.cumsum()` 逐步累加
  - 例如：`[0.2881, 0.4710, 0.6071, ...]`
- `cumulative_variance >= 0.95` — 判断哪个位置开始超过95%，返回True/False数组
- `np.argmax(...)` — 找第一个True的索引（从0开始）
- `+ 1` — 索引从0开始，所以要+1变成组件数量（人类计数）
- **变量** `n_components_95`：达到95%方差需要的主成分数量（答案是7）

```python
ax2 = ax1.twinx()
```
- `twinx()` — 创建共享X轴的第二个Y轴（右侧Y轴）
- 这样可以在同一张图上画两种量纲不同的数据（左轴=单个方差比例，右轴=累计方差）
- **变量** `ax2`：右侧Y轴对应的坐标系

```python
lines1, labels1 = ax1.get_legend_handles_labels()
lines2, labels2 = ax2.get_legend_handles_labels()
ax1.legend(lines1 + lines2, labels1 + labels2, loc="center right")
```
- 因为有两个坐标轴，图例默认只显示一个轴的
- 这三行把两个轴的图例合并到一个框里显示

---

## 第七部分：特征选择（Section 4.0）

### 互信息（Mutual Information）

```python
mi_scores = mutual_info_classif(X_scaled, y, random_state=42)
```
- `mutual_info_classif()` — 计算每个特征和目标变量之间的互信息
- 互信息含义：知道这个特征的值，能减少多少对目标（糖尿病）的不确定性
- 分数越高 = 特征越重要
- `random_state=42` — 固定随机种子，保证每次结果相同（42是惯例）
- **变量** `mi_scores`：长度为8的数组，每个特征对应一个分数

```python
mi_df = (pd.DataFrame({"Feature": feature_cols, "MI Score": mi_scores})
         .sort_values("MI Score", ascending=False)
         .reset_index(drop=True))
```
- 把特征名和分数合并成表
- `.sort_values("MI Score", ascending=False)` — 按分数从高到低排序
- `.reset_index(drop=True)` — 排序后重置行号
- **变量** `mi_df`：特征MI分数排名表

```python
mi_threshold = mi_df["MI Score"].median()
mi_selected_features = mi_df.loc[mi_df["MI Score"] > mi_threshold, "Feature"].tolist()
```
- **变量** `mi_threshold`：MI分数的中位数，作为筛选阈值（0.1096）
- `mi_df["MI Score"] > mi_threshold` — 找出分数高于中位数的行
- `.loc[条件, "Feature"]` — 取满足条件的"Feature"列
- `.tolist()` — 转成Python列表
- **变量** `mi_selected_features`：被选中的特征列表
  - 结果：`['DiabetesPedigreeFunction', 'BMI', 'Glucose', 'Insulin']`（4个）

```python
colors_mi = [sns.color_palette("tab10")[0] if f in mi_selected_features
             else sns.color_palette("tab10")[3] for f in mi_df["Feature"]]
```
- 列表推导式：遍历每个特征，如果被选中就用蓝色，否则用红色
- **变量** `colors_mi`：颜色列表，和 `mi_df` 的行一一对应

```python
ax.axvline(mi_threshold, color="black", linestyle="--", linewidth=1.5, ...)
```
- `axvline()` — 画一条竖直虚线，标出筛选阈值的位置

---

### RFE（递归特征消除）

```python
rfe_estimator = LogisticRegression(max_iter=1000, random_state=42)
rfe = RFE(estimator=rfe_estimator, n_features_to_select=5)
rfe.fit(X_scaled, y)
```
- **变量** `rfe_estimator`：作为"评判官"的逻辑回归模型
- **变量** `rfe`：RFE对象
  - `n_features_to_select=5` — 最终保留5个特征
- `rfe.fit()` — 训练过程：
  1. 用全部8个特征训练逻辑回归
  2. 找出权重最小的特征，删除它
  3. 用剩下的7个特征重新训练
  4. 重复直到只剩5个
- `max_iter=1000` — 逻辑回归最多迭代1000次（防止因为收敛慢而报错）

```python
rfe_df = pd.DataFrame({
    "Feature": feature_cols,
    "Selected": rfe.support_,
    "Ranking": rfe.ranking_
})
```
- `rfe.support_` — 布尔数组，True表示该特征被选中（排名为1）
- `rfe.ranking_` — 整数数组，1=被选中，数字越大=越早被删除
- **变量** `rfe_df`：RFE结果表

```python
rfe_selected_features = rfe_df.loc[rfe_df["Selected"], "Feature"].tolist()
```
- 筛选出 `Selected=True` 的行，取特征名
- **变量** `rfe_selected_features`：RFE选出的特征
  - 结果：`['Age', 'BMI', 'DiabetesPedigreeFunction', 'Glucose', 'Pregnancies']`（5个）

```python
bar_colors = rfe_plot_df["Selected"].map({True: "#2ca02c", False: "#d62728"})
```
- `.map({True: 绿色, False: 红色})` — 把True/False映射成颜色代码
- `#2ca02c` — 绿色（被选中）
- `#d62728` — 红色（被淘汰）
- **变量** `bar_colors`：每个条形对应的颜色

```python
selected_patch = plt.Line2D([0], [0], marker="s", color="w",
                             markerfacecolor="#2ca02c", markersize=12, label="Selected")
rejected_patch = plt.Line2D([0], [0], marker="s", color="w",
                             markerfacecolor="#d62728", markersize=12, label="Rejected")
ax.legend(handles=[selected_patch, rejected_patch])
```
- 手动创建图例：画一个看不见的线段，只用它的方块标记当图例
- `marker="s"` — 方块形状（square）
- `color="w"` — 线条白色（不可见）
- `markerfacecolor` — 方块填充颜色
- 这样图例才能显示绿色=Selected，红色=Rejected

---

## 第八部分：模型评估（Section 5.0）

```python
model = LogisticRegression(max_iter=1000, random_state=42)
cv = StratifiedKFold(n_splits=5, shuffle=True, random_state=42)
```
- **变量** `model`：逻辑回归分类器
- **变量** `cv`：5折分层交叉验证策略
  - `n_splits=5` — 把数据分成5份
  - `shuffle=True` — 分割前先打乱顺序（避免数据有顺序规律）
  - 分层（Stratified）— 每一份里，有糖尿病/没糖尿病的比例和原数据相同
  - 为什么5折？比单次分割更稳定，能看到模型在不同数据子集上的表现

```python
scorers = {
    "Accuracy":  make_scorer(accuracy_score),
    "Precision": make_scorer(precision_score, zero_division=0),
    "Recall":    make_scorer(recall_score,    zero_division=0),
    "F1":        make_scorer(f1_score,        zero_division=0),
}
```
- **变量** `scorers`：字典，4种评价指标
- `make_scorer()` — 把评价函数包装成 `cross_val_score` 能用的格式
- `zero_division=0` — 如果某折里某类别没有预测，分母为0时返回0而不是报错

四个指标的含义：

| 指标 | 公式 | 含义 |
|---|---|---|
| Accuracy（准确率） | 预测正确数 / 总数 | 整体正确率 |
| Precision（精确率） | 真正阳性 / 预测为阳性的总数 | 预测有糖尿病的里，真正有的比例 |
| Recall（召回率） | 真正阳性 / 实际阳性总数 | 实际有糖尿病的人，被正确找出的比例 |
| F1 | 2 × P × R / (P + R) | 精确率和召回率的调和平均，综合指标 |

```python
def evaluate_feature_set(name, selected_features):
    row = {"Scenario": name, "Features Used": ", ".join(selected_features)}
    for metric, scorer in scorers.items():
        scores = cross_val_score(model, X_scaled[selected_features], y,
                                 cv=cv, scoring=scorer)
        row[metric] = round(scores.mean(), 4)
    return row
```
- **函数** `evaluate_feature_set`：给一组特征做5折交叉验证并返回结果
- 参数 `name`：场景名称（比如"All Features"）
- 参数 `selected_features`：使用哪些列
- `cross_val_score()` — 做5折交叉验证，返回5个分数（每折一个）
- `scores.mean()` — 取5次的平均分，更稳定
- `row`：字典，存储这个场景的所有指标结果

```python
performance_rows = [
    evaluate_feature_set("All Features (Baseline)", feature_cols),
    evaluate_feature_set("MI-Selected Features",    mi_selected_features),
    evaluate_feature_set("RFE-Selected Features",   rfe_selected_features),
]
performance_df = pd.DataFrame(performance_rows)
```
- **变量** `performance_rows`：三个场景的结果列表
- **变量** `performance_df`：三行四列的结果表（3种特征集 × 4个指标）

实际结果：

| 场景 | Accuracy | Precision | Recall | F1 |
|---|---|---|---|---|
| 全部特征（基线） | 0.7673 | 0.7059 | 0.5557 | 0.6216 |
| MI选出的特征 | 0.7706 | 0.7222 | 0.5410 | 0.6180 |
| RFE选出的特征 | 0.7713 | 0.7163 | 0.5557 | 0.6257 |

```python
plot_df = performance_df.melt(
    id_vars=["Scenario", "Features Used"],
    value_vars=["Accuracy", "Precision", "Recall", "F1"],
    var_name="Metric", value_name="Score"
)
```
- 再次把宽表转长表，用于 `sns.barplot` 的分组条形图
- `id_vars` — 保持不变的列（每行重复出现）
- `value_vars` — 要"融化"成行的列（4个指标）
- **变量** `plot_df`：每行代表"某场景的某指标的分数"，共12行（3×4）

```python
for container in ax.containers:
    ax.bar_label(container, fmt="%.3f", fontsize=8, padding=2)
```
- `ax.containers` — 图中所有条形的集合
- `bar_label()` — 在每个条形顶端显示数字标签
- `fmt="%.3f"` — 保留3位小数

```python
preprocessed_output = X_scaled.copy()
preprocessed_output[target_col] = y.reset_index(drop=True)
preprocessed_output.to_csv(OUTPUT_DIR / "preprocessed_dataset.csv", index=False)
```
- 把标准化后的特征和目标列合并，保存为 CSV
- `y.reset_index(drop=True)` — 重置索引保证和 `X_scaled` 的行号对齐
- `index=False` — 不把行号写进文件（避免多一列无意义的数字）
- 保存路径：`output/preprocessed_dataset.csv`

---

## 变量总览速查表

| 变量名 | 类型 | 含义 |
|---|---|---|
| `DATA_DIR` | Path | 数据文件夹路径 `data/` |
| `OUTPUT_DIR` | Path | 输出文件夹路径 `output/` |
| `FIG_DIR` | Path | 图片文件夹路径 `output/figures/` |
| `missing_zero_cols` | list | 用零代表缺失的5列名称 |
| `feature_cols` | list | 8个输入特征的列名 |
| `target_col` | str | 目标列名 `"Outcome"` |
| `df_pima` | DataFrame | PIMA原始数据（768行×9列） |
| `df_frankfurt` | DataFrame | Frankfurt原始数据（2000行×9列） |
| `zero_counts_before` | DataFrame | 各列0值数量统计 |
| `missing_counts_after` | DataFrame | 替换后NaN数量统计 |
| `df_combined` | DataFrame | 合并后的数据（2768行×10列） |
| `df_model` | DataFrame | 删除source列后的建模数据（2768行×9列） |
| `X_original` | DataFrame | 原始特征矩阵（未变换） |
| `y` | Series | 目标变量（Outcome列） |
| `log_cols` | list | 需要对数变换的2列 |
| `X_transformed` | DataFrame | 对数变换后的特征矩阵 |
| `before_log` | DataFrame | 对数变换前的数据（画图用） |
| `after_log` | DataFrame | 对数变换后的数据（画图用） |
| `scaler` | StandardScaler | 标准化器对象 |
| `X_scaled_array` | ndarray | 标准化后的numpy数组 |
| `X_scaled` | DataFrame | 标准化后的特征矩阵（主要使用） |
| `before_melted` | DataFrame | 标准化前的长表格式（画箱线图用） |
| `after_melted` | DataFrame | 标准化后的长表格式（画箱线图用） |
| `pca` | PCA | PCA对象 |
| `explained_variance` | ndarray | 每个主成分的方差比例 |
| `cumulative_variance` | ndarray | 累计方差比例 |
| `n_components_95` | int | 达到95%方差需要的主成分数量（=7） |
| `variance_df` | DataFrame | PCA方差信息表 |
| `mi_scores` | ndarray | 8个特征的互信息分数 |
| `mi_df` | DataFrame | 特征按MI分数排序的表 |
| `mi_threshold` | float | MI分数中位数（=0.1096），筛选阈值 |
| `mi_selected_features` | list | MI选出的特征（4个） |
| `rfe_estimator` | LogisticRegression | RFE使用的基分类器 |
| `rfe` | RFE | RFE对象 |
| `rfe_df` | DataFrame | RFE排名结果表 |
| `rfe_selected_features` | list | RFE选出的特征（5个） |
| `model` | LogisticRegression | 最终评估用的分类器 |
| `cv` | StratifiedKFold | 5折分层交叉验证策略 |
| `scorers` | dict | 4种评价指标的scorer字典 |
| `performance_rows` | list | 三个场景的评估结果列表 |
| `performance_df` | DataFrame | 模型性能对比表（3行×6列） |
| `plot_df` | DataFrame | 画图用的长表格式（12行×4列） |
| `preprocessed_output` | DataFrame | 保存到CSV的最终数据 |

---

*生成时间：2026-04-26 | 课程：TC6414 | 学号：P162614*
